# Claude Code Instructions

Read and follow `AGENTS.md` and `START_HERE.md` before editing this vault.

`AGENTS.md` is the shared operating contract for every harness. Keep `raw/` immutable, write reusable knowledge in `wiki/`, update `wiki/Log.md` for meaningful changes, and ask before destructive, external, credential, cost-incurring, or runtime-affecting actions.
